# 보안 범위

## 현재 보호 장치

- 개인정보 조회·수정 API는 서버 세션 인증을 요구합니다.
- 공개 대기 현황에는 접수번호와 상태만 반환합니다.
- PIN·복구 코드는 scrypt 해시로 저장합니다. 로그인 실패 제한과 PIN 변경 시 세션 종료를 적용합니다.
- LAN 공개 실행에는 TLS 인증서와 개인 키가 필요합니다. HTTPS 쿠키에는 Secure, HttpOnly, SameSite=Strict를 적용합니다.
- 다른 출처에서 들어오는 변경 요청을 차단합니다.
- SQLite 트랜잭션과 기록 버전 검사로 저장 및 수정 충돌을 처리합니다.
- 내보내는 백업은 암호화하며 DB 원본 암호화와는 구별합니다.

## 남은 한계

담당자는 공용 PIN을 사용합니다. 간호사·근로학생별 권한과 개인별 감사 추적은 구현하지 않았습니다.
서버 관리자나 DB 파일 접근자는 기록과 감사 로그를 읽거나 수정할 수 있습니다. 감사 기록은 변경 불가능한 로그가 아닙니다.
클라이언트의 주기 조회도 세션 만료를 연장하므로 서버 세션 만료만으로 사용자 무활동을 판정하지 않습니다. 화면에서 별도로 무활동 잠금을 수행합니다.
초기 PIN 설정 API는 최초 설정 전에는 접근 가능합니다. 서버 PC에서 최초 설정을 완료한 뒤 LAN 접근을 허용하세요.

서버를 인터넷에 직접 공개하지 말고, 승인된 기기만 방화벽에서 허용하세요. TLS 인증서 신뢰, 계정·디스크 보호, 백업 보관과 복원 확인은 현장 설정 사항입니다.
실제 기록·PIN·복구 코드·개인 키를 GitHub 이슈나 커밋에 첨부하지 마세요. 취약점은 개인정보를 제거한 재현 절차와 함께 저장소 관리자에게 비공개로 전달하세요.
